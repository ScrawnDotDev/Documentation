---
title: Collect Payments
description: Generate checkout links to collect payments from users
---

## Overview

The `collectPayment` method generates a checkout link for users to complete their payment. This integrates with your configured payment provider (e.g., LemonSqueezy) to handle the payment flow.

## Basic Usage

Generate a checkout link for a user:

```typescript
import { biller } from "./scrawn/biller";

const checkoutLink = await biller.collectPayment("user-123");
```

Redirect them to the payment page:

```typescript
// In your route handler (Express, Next.js, etc.)
res.redirect(checkoutLink);
```

<Callout type="info">
**In simple terms:** Give me a payment link for user `user-123` where they can go and pay all their outstanding amount (which is being tracked by scrawn).
</Callout>

## Parameters

<TypeTable type={{
  userId: {
    description: "The unique identifier of the user to collect payment from",
    type: "string",
    required: true,
  },
}} />

## Response

<TypeTable type={{
  checkoutLink: {
    description: "A checkout URL where the user can complete their payment",
    type: "Promise<string>",
  },
}} />


## Next Steps

<Cards>
  <Card href="/scrawn.js/functions/usage-tracking" title="Usage Tracking">
    Learn how to track billable events
  </Card>
  <Card href="/scrawn.js/functions/ai-token-billing" title="AI Token Billing">
    Meter AI token usage per-call
  </Card>
</Cards>
